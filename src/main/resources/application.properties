spring.application.name=balmerrestservice

server.port=8080
#logging.level.org.springframework.web=DEBUG
#server.servlet.context-path=/balmerrestservice
#
#management.endpoints.web.exposure.include=*
#management.endpoint.web.requestmapping.enabled=true

# ===========================================
# Centralized Server Configuration
# ===========================================
# Change these values to switch servers - only need to edit once
ibps.server.host=74.225.130.20
ibps.server.port=8080
ibps.base.url=http://${ibps.server.host}:${ibps.server.port}/iBPSRestFulWebServices/ibps/Restful/fosasoft
ibps.cabinet.name=fosasoft

omnidocs.server.host=${ibps.server.host}
omnidocs.server.port=${ibps.server.port}
omnidocs.base.url=http://${omnidocs.server.host}:${omnidocs.server.port}/OmniDocsRestWS/rest/services

db.server.host=${ibps.server.host}
db.server.port=1433

# ===========================================
# Session Cache Configuration
# ===========================================
# Session cache timeout in minutes (iBPS default is 30 min, using 25 for safety buffer)
session.cache.timeout.minutes=25

# ===========================================
# Database Configuration
# ===========================================
spring.datasource.url=jdbc:sqlserver://${db.server.host}:${db.server.port};encrypt=true;trustServerCertificate=true;databaseName=${ibps.cabinet.name};
spring.datasource.username=sa
spring.datasource.password=Sedin@123456
spring.datasource.driver-class-name=com.microsoft.sqlserver.jdbc.SQLServerDriver

# ===========================================
# iBPS API URLs (using centralized base URL)
# ===========================================
ibps.wmconnect.url=${ibps.base.url}/WMConnect/?engineName=%s&userName=%s&UserExist=N
ibps.fetchAttributes.url=${ibps.base.url}/WMFetchWorkItemAttributes/?engineName=%s&processInstanceId=%s&workitemId=%d
ibps.uploadWorkItem.url=${ibps.base.url}/WFUploadWorkItem/
ibps.getWorkItem.url=${ibps.base.url}/WMGetWorkItem/?engineName=%s&processInstanceId=%s&workitemId=%d
ibps.getUnlockWorkItem.url=${ibps.base.url}/WMUnlockWorkitem/?engineName=%s&processInstanceId=%s&workitemId=%d
ibps.fetchWorkList.url=${ibps.base.url}/WMFetchWorkList/?engineName=%s&queueId=%d
ibps.reassignWorkItem.url=${ibps.base.url}/WMReassignWorkItem/?engineName=%s&processInstanceId=%s&workitemId=%d&targetUser=%s&OpenMode=%s&comments=%s
ibps.disconnect.url=${ibps.base.url}/WMDisconnect?engineName=%s&name=%s
ibps.getCompleteWorkItem.url=${ibps.base.url}/WMCompleteWorkitem/?engineName=%s&processInstanceId=%s&workitemId=%s
wf.setattributes.url=${ibps.base.url}/WFSetAttributes/?engineName={engineName}
ibps.getProcessList.url=${ibps.base.url}/WMGetQueueList/?engineName=%s
ibps.getQueueList.url=${ibps.base.url}/WMGetQueueList/?engineName=%s
ibps.fetchMyQueueWorkItems.url=${ibps.base.url}/WMFetchWorkList/?engineName=%s&queueId=0

# ===========================================
# OmniDocs API URLs (using centralized base URL)
# ===========================================
omnidocs.api.url=${omnidocs.base.url}/executeAPIJSON
omniadddocument.api.url=${omnidocs.base.url}/addDocumentJSON
omnigetdocument.api.url=${omnidocs.base.url}/getDocumentStreamJSON

# ===========================================
# Notesheet Configuration
# ===========================================
notesheet.temp.directory=./tmp/notesheets
notesheet.document.class=notesheet_original

# ===========================================
# Document Viewer Configuration
# ===========================================
# Base URL for document viewer links in PDF (used for hyperlinks)
docs.viewer.server.host=74.225.130.20
docs.viewer.server.port=8090
docs.viewer.context.path=/balmerrestservice
docs.viewer.base.url=http://${docs.viewer.server.host}:${docs.viewer.server.port}${docs.viewer.context.path}/docs/viewer

# ===========================================
# Service Account (for auto-authentication)
# ===========================================
service.account.username=supervisor
# Encrypted with AES (Key: BalmerLawrie2025)
service.account.password=JqVxhLUw0FWr1qD9YNpsvQ==